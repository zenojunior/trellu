{
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "heroku:bash": "heroku run bash --app=trellu-app",
    "heroku:log": "heroku logs --tail --app=trellu-app",
    "postinstall": "npm install --prefix=api",
    "start": "ENV_SILENT=true npm start --prefix=api"
  }
}